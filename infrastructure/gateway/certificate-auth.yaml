# Companion to certificate-grafana.yaml, which records why these are two
# separate certificates rather than one SAN certificate.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-tls
  namespace: gateway
spec:
  secretName: auth-tls
  duration: 720h
  renewBefore: 240h
  privateKey:
    algorithm: ECDSA
    size: 256
    rotationPolicy: Always
  dnsNames:
    - auth.aegis.test
  issuerRef:
    name: aegis-dev-ca
    kind: Issuer
    group: cert-manager.io
