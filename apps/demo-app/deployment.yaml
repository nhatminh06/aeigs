apiVersion: apps/v1
kind: Deployment
metadata:
  name: podinfo
  namespace: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: podinfo
  template:
    metadata:
      labels:
        app: podinfo
    spec:
      automountServiceAccountToken: false
      containers:
        - name: podinfo
          image: ghcr.io/stefanprodan/podinfo:6.14.1
          ports:
            - name: http
              containerPort: 9898
          envFrom:
            - secretRef:
                name: podinfo-ui
          securityContext:
            # The image's Dockerfile sets USER app (non-numeric), which
            # the kubelet can't verify against runAsNonRoot on its own —
            # the numeric uid is required explicitly (id -u app == 100).
            runAsNonRoot: true
            runAsUser: 100
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
