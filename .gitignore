# Database backup artifacts must never be committed — their real home is
# ~/.local/share/aegis/backups/ (outside this repository entirely). This
# is belt-and-suspenders in case a script is ever run from inside the
# repo directory by mistake.
*.dump
*.dump.age
