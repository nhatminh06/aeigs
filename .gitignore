# OS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# kind / kubectl local state
kubeconfig
*.kubeconfig

# generated by scripts/git-bridge-up.sh (local dev-only Flux git source)
.git-bridge/

# age / SOPS key material — never commit private keys
*.agekey
age.key

# Decrypted secrets that should only ever exist locally
*.decrypted.yaml
*.decrypted.yml
