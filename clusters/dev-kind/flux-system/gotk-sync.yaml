# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/nhatminh06/aeigs
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  # flux bootstrap hardcodes 10m here; shortened for dev so config
  # experiments converge quickly. `flux bootstrap` re-runs would reset
  # this back to 10m0s.
  interval: 1m0s
  path: ./clusters/dev-kind
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
